# 进度与下一步

> 最后更新：2026-08-22（当天第二次，场景部分收尾）
> 这份文件是**接着干**用的：今天停在哪、明天从哪起手。
> 长期的方法和规则不写在这里，写在 `styles/<风格>/README.md` 和
> `docs/环境表面制作方法.md` 里。

## 当前站位

里程碑 M2（视觉）。**场景部分告一段落，下一步做角色。**

```
tools/            共用：.tscn 生成器、Blender 工具、测量脚本
styles/gatling/   风格一（Gatling Gears 那种写实低模）—— 已完成，未受近期改动影响
styles/hellrider/ 风格二（BoomBit Hellrider 那种极简平面低模）—— 场景已成型
```

`styles/hellrider/layout.py` 现在有**三个模式**，优先级从高到低：

| 开关 | 是什么 | 用途 |
|---|---|---|
| `BIGMAP = True` | **现在的默认。** 8×8 屏大地图、四面包熔岩、成簇随机撒、方向键漫游 | 看整体、看疏密 |
| `MINIMAL = True` | 地面 + 熔岩 + 3 组石头 + 4 棵树，位置写死 | **调形状/明暗**：改前改后必须看到同一个东西 |
| 两个都 False | 完整村庄（河、桥、房子） | 还在，但已不是主线 |

跑大地图：

```
python tools/gen_scene.py --style hellrider
"C:/Godot4.7/Godot_v4.7-stable_win64.exe" --path . res://scenes/VisualBenchmark_hellrider.tscn
```

方向键平移，Shift 加速，**R 重掷整张图**。
截图走 `bash tools/hr_map_shot.sh out.png pos=x,z [ortho=N]`。

当前渲染图：`docs/reference/m2_visual_hellrider.png`

## 今天（8/22）做完的

十五个提交，四组事。细节都在提交信息和 `styles/hellrider/README.md` 里。

**一、地面的明暗**（`bdb5e72` `3cd5648`）
格子之上加了一层低频分区。判据是 `tools/hr_ground_stats.py`：只取画面里的
空地，和参考图同口径比。总 std 3.26 → 6.0 左右，落进参考区间 5.5–6.1。
中途把色格转成 45°，**是错的**，已改回轴向 —— 并把"正面 90°、轴向、地面全平"
提升成 README 的**第 4 条不能破的规则**（它原来只写在 `layout.py` 开头）。

**二、面的明暗：三条规则压成一条**（`f3ccc09` `837ad52` `17cfa58`）
亮度只由面法线和光照方向的**夹角 θ** 决定。半 Lambert、档内微分、
`step_weight` 三个补丁全删了 —— 它们都是在补 `dot()` 的毛病。
真正把"暗面不够深"解决掉的是**把四档瞄准相机看得见的那段 θ**（12–105°）：
摊在整个半球上时最暗一档全给了看不见的底面。侧/顶 0.86 → 0.68（参考 0.59/0.72）。

**三、石头的造型**（`80e6b6c` `0226ae4` `7660f2a`）
压掉又深又窄的凹折（`deconcave`）；边数按尺寸分级；第三环只给最大的块；
把法线相近的相邻面并成一个真正的平面（`flatten`）。
最碎的从 404 px²/面 到 919。

**四、大地图**（`da73263` 及其后五个修）
`HellMap.gd` + `ClusterScatter.gd` + `MapInspector.gd`。
**这个模式下没有任何东西是在 Python 里摆死的**，全在运行时生成。

今天真正留下来的其实是**六个判据脚本**：

```
tools/hr_ground_stats.py   地面空地的明暗分布
tools/hr_concave.py        又深又窄的凹折
tools/hr_face_px.py        每个面在屏幕上有多少像素
tools/hr_face_jump.py      相邻面的非比例跳变
tools/hr_shot.sh           MINIMAL：生成 + 渲染 + 量
tools/hr_map_shot.sh       BIGMAP：重新导入 + 生成 + 渲染
```

## 明天的第一件事：角色

现在场景里的"单位"只有一个纯黄方块（`_bigmap` 里的 `Player`），纯尺度参照。

**已有的东西：**

- `scripts/units/EnemyCrowd.gd` / `EnemyCrowdMM.gd` / `CombatDirector.gd` ——
  M4/M5 的产物，为 **gatling** 风格写的。MultiMesh 那条路已经验证过了。
- `styles/hellrider/materials.py` 里 `player` / `enemy` / `enemydark` 三个材质
  已经配好（`hr_flat`，`tint = 0` 用纯色，`light_steps = 3`）。
- `EnemyCrowd.spawn_radius` 是**屏幕外生成半径**，`layout.spawn_radius()`
  按相机参数推导：正交 40° 下是 48.1 m。

**需求文档 §4.1 定的小型敌人：**

    同屏 500–1000（压测 1500–2000+）
    100–300 三角形 / 单位
    3–8 个机械构件（主体 / 腿 / 武器 / 炮塔 / 轮子）
    **避免传统骨骼动画** —— 走程序化 Transform、刚体构件、Shader 动画
    最终形态是 Data-Oriented + MultiMesh，不是 1000 个完整 SceneTree

**风格上不能破的（都在 `styles/hellrider/README.md`）：**

- 单位也是**纯色块**：没有贴图、没有法线扰动，明暗必须分档
- 轴向、地面全平
- 影子走 blob 贴片，不用真实投影。**`blob_scale` 有算得出来的下限**：
  blob 是边长 `blob_scale × 缩放` 的方片，半宽 = `blob_scale/2`；
  物体半宽超过它，整片影子就藏在物体底下 —— 今天这个坑复发了三次

**参考图里的角色**：`styles/hellrider/reference/` 里的骷髅兵和摩托车手 ——
骷髅约 1.8 m，是全套尺度的锚。我们的方块是 1.6 m。

## 今天走过但否决的路

- **色格转 45°**。理由当时是"熔岩外侧那层装饰菱形本来就是 45°"。是错的：
  参考图的菱形是**等距投影**的产物，照抄角度等于照抄了一台我们没有的相机。
  同一个陷阱今天还在余烬上重犯了一次（随机 yaw 的斜方块）。
- **生成场景时按最终 yaw 烘方向性明暗**。前提是成立的（道具摆下去就不动），
  但量过之后不值：邻面跳变只影响 1.6% 的边，而代价是散布物的 MultiMesh
  合批全没了。数字在 README 的「已知待办」里。

## 存着的一条岔路：换造型语言

`docs/造型语言试验.md` —— 2026-08-22 试过"大小不一的方块 + 边角加亮"那套
（RTS 常见的语言）。**在 90° 正视下成立**，前提是墙面朝向要散开：
树靠转朝向（yaw 20–70°），山靠**平面轮廓带 30/45/60° 斜边**（墙仍垂直）。

不倒角只要 42–100 tris（现在的平滑低模是 ~30），而生成代码简单得多 ——
`deconcave` / `flatten` / 面数分级 / 底面椭圆这些今天花时间最多的东西
在那套语言里根本不存在。而且我们的地面本来就是方格、熔岩是方块，其实更搭。

代码在 `styles/hellrider/experiments/`，图在 `docs/reference/斜边台地_*.png`。
**先不采纳**：换语言等于不再跟 Hellrider 参考图，实测校准要重推一遍。

## 悬着的问题

- **大石头还是比参考图碎**：`rock_big.0` 17.2 个可见面 / 668 px²，
  参考图约 2500 px²/面。它的面朝向差本来就大于 30°，再并要动生成规则。
- **参考图的石头比我们更方**：墙近乎垂直、顶是平台加台阶；
  我们 `taper = 0.30`，侧壁内收得多，读成穹顶多过读成盒子。
- 河岸那些白石头的下部面被 `low_cap` 压成近黑，读起来像黑白花的。
  白色底色下 `low_cap = 0.30` 太狠。
- 地面的土路（`hr_ground` 留了 `path_mask` 接口，还没接遮罩图）；
  参考图里还有踩出来的拖痕、小草丛。
- **高光贴图**（8/21 问到、一直没做）：Godot 4 没有 specular 贴图槽，
  只有 `roughness_texture` / `metallic_texture`。该走"Python 直接生成 PNG"
  那条路。只对 **gatling** 有意义（hellrider 禁贴图）。
- 需求文档 §11 的目标硬件（集显）实测还没做 —— 手头没有这类机器。
  大地图的实际开销也还没量（现在是 20–30 个 draw call，但没做过正经 benchmark）。
- 需求文档 §21 的外部资产管线验证还没决定用哪套资产。

## 2026-08-24：开了第三个 Demo（数值验证）

美术方向的两个 Demo 到此告一段落，工程加了一条和它们完全平行的线：
`sim/`（纯逻辑规则）+ `data/`（JSON 数值）+ `demos/03_balance/`（手玩壳 / headless 批量）。
入口换成 `demos/Launcher.tscn`，三个 Demo 各走各的，旧的 `scenes/` `shaders/`
`benchmark/` 一个文件没动。

结构、规矩和怎么跑见 `docs/Demo3_数值验证_工程说明.md`。
现在是设计文档 §22 的 Phase 1，首轮数值还没调 —— 15 分钟局长存活率 0%。

## 2026-08-26：Demo 3 的 P1 / P2 完成

**P1 环形地图与机甲运动**
- `sim/core/Torus.gd`：200×100 上下左右接边。**sim/ 里所有距离和方向都走它**，
  已经确认没有残留的 `distance_to()`/`direction_to()`。
- `SpatialHash` 按地图尺寸取模分桶；查询半径大到绕地图一圈时退化成全取，避免重复计数。
- 机甲：血量 1000、移速 2 格/秒、90° 转向 1 秒、**转向期间 `can_fire()` 为 false**。
- 手玩视图改成相机跟随：屏幕中心恒为机甲，其余按环形最短向量绘制，接缝在画面上不可见。

**P2 波次系统（形状 × 成长）**
- `data/waves.json` 用两段式：形状固定波型性格、成长是唯一跨周期旋钮，四项全 ≥ 1。
  设计文档 §4.5 已同步改写成同一套写法。
- 四种生成方式 random / group / circle 实现完毕（line 留着没用到），
  运动方式 chase / straight，攻击方式 melee / ranged（子弹速度 1.5、进射程钉住）。
- `DataDB` 会校验 growth 是否有小于 1 的项并报警——这是设计上的硬要求。

**验收**
- `SelfCheck.gd`：12 项全过（环形几何、角度约定、走一圈回原点偏差 0.0024 格、
  同 seed 逐字节一致、不同 seed 不同）。
- `WaveCheck.gd`：实现生成的敌人和 `tools/sim/wave_shape.py` 的推演表逐波对上
  （周期 1 数量 20/30/30/2/40/50/200/50，血量 100/110/132/2000/99/99/20/119）。
- 批量模拟 155 倍速（敌人比之前多得多，比 590 倍速慢是预期的）。

**下一步 P3**：录入枪系 1-13 号的真实数值、槽位攻击弧、四种攻击形态、六种索敌方式。
现在还挂着占位武器（单发枪 14 伤害），所以击杀数只有个位数，那不是数值问题。

## 2026-08-26：P3 完成（枪系武器 + 攻击形态 + 六种索敌）

- `data/weapons.json` 录入枪系 1-13 号，字段名和 §7.8 一一对应
  （attack_shape / damage / interval / range / aoe / targeting / upgrade / mechanic / ultimate）。
  ultimate 段先录着不实现，留给 P8。
- `sim/systems/Targeting.gd`：六种索敌。1-4 号已实现；**5、6 号要读 §7.6 的地图评估，
  P4 之前退化成 1 号，并且 DataDB 会在加载时报警**——不报警的话两种武器会悄悄变成同一种。
- 四种攻击形态：single / multi / area（圆形 AoE）/ line（沿途每个吃 20%，
  §7.8 穿刺枪）。击退和连射（Fragment Cannon / Wall of Lead 一次 3 发、可切目标）也做了。
- 弹丸速度文档没给，统一按 30 格/秒（几乎瞬时命中），免得弹道成为隐藏变量。

**验收**：`SelfCheck.gd` 19 项全过，新增的 P3 部分包括——
槽位弧心与 §6.4 的角度表逐个对上；索敌 1/2/3/4 各自选中预期目标；
锁定型目标没死不换、死了才换；范围形态一发打到 5 个；线性形态打穿一列 4 个且沿途正好 20%；
击退推开 0.75 格。

`tools/sim/dps_check.py` 把 data 里的数值和文档自己写的 DPS 列对了一遍，13 把里 12 把吻合。

**一处文档内部对不上**：Spread Gun 写的是伤害 140 / 间隔 2.5 秒，算出来 56 DPS，
但它自己的 DPS 列写的是 70。要么伤害改 175，要么间隔改 2.0 秒。已按文档的
伤害/间隔录入（56），等确认。

**性能**：批量模拟从 155 倍速掉到 36 倍速（敌人几百个 + 弹丸变多）。
P7 要扫大批参数时得先优化，目前够用。

**下一步 P4**：§7.6 的地图状态评估（1Hz、9 区域、威胁/DPS/最高血量/聚集度），
做完索敌 5、6 号才能真正生效，自动模拟的 AI 也要靠它。

## 2026-08-26：P4 完成（地图状态评估 §7.6）

`sim/systems/MapEval.gd`，1 秒跑一次，按文档的做法**以机甲当前中心为格心重新划格**
（29×29 的局部盘，外边线 = 车体半宽 1.5 + 向外 12 格）。产出：

- 九宫格分区的威胁 DPS / 敌人血量 / 数量；近战按"离车体边缘第几圈"衰减
  （第 1 圈 100%、每远一圈 -10%、10 圈以外忽略），远程按"超出自己射程几格"衰减；
- 四方向汇总（斜角各按 50% 计入两个正方向）；
- 四方向的己方 DPS（炮塔弧覆盖到该方向才算）与清场秒数；
- 高血量敌人 top-K（阈值 = 当前最高血量的 50%）；
- 3×3 / 5×5 的敌人聚集度 top-K。

**§7.6.3 的量纲对齐**：文档写的是"DPS 超过这个方向敌人的血量"，DPS 是每秒、
血量是总量，实现里按"清场秒数 = 该方向敌人总血量 ÷ 该方向己方 DPS"来算。

索敌 5（范围杀伤最大）和 6（对自己 DPS 最高）接上了，DataDB 不再报"未实现"。

**性能：先量再改。** 初版 1000 敌人要 6.7ms。凭感觉优化两轮只降到 2.8ms，
分段计时才发现真正的热点是 `top_hp` 的全量 `sort_custom`（1093 µs，占一半）——
1000 只同血量敌人全部过阈值，带 lambda 的排序就是 1ms。换成定长 top-K 插入后
整体降到 **993 µs**。主循环（756 µs / 1000 只）现在是大头，已经把环形 delta、
九宫格归属、威胁衰减全部内联，GDScript 的跨对象调用开销比算术贵得多。
分段计时脚本留在 `demos/03_balance/EvalProf.gd`。

判据定在 1.5ms（1000 只全在盘内是压力值，实战峰值约 100-300；
1 Hz × 批量 36 倍速 ≈ 4% 开销）。

**验收**：SelfCheck 现在 27 项全过，新增 P4 部分包括九宫格计数、四方向汇总、
近战按圈衰减（第 5 圈 = 60%）、高血量阈值筛选、分方向己方 DPS（北槽只算上方不算下方）、
清场秒数、索敌 5 选扎堆的、索敌 6 选打得疼的、以及性能预算。

**注意**：这一步不改变现有战斗结果（282.1s / 47.1s 和 P3 完全一致），
因为枪系 1-13 用的索敌是 1/3/4，没有 5/6。MapEval 真正发挥作用要等下一步的 AI。

**下一步**：自动模拟的 AI（之前讨论过的三层：1Hz 战略方向 / 4Hz 短程试探 /
每 tick 势场微调 + 转向的效用比较），以及 `stand`/`line`/`field`/`oracle` 四档策略谱系。

## 2026-08-26：自动模拟的 AI（四档策略谱系）

`data/agents.json` + `sim/agent/ScriptedAgent.gd`。移动三层（1Hz 战略方向读 §7.6 /
4Hz 短程试探 / 每 tick 势场微调），转向走效用比较而不是"敌人在哪转哪"。
实现技巧：机甲转 90° 时所有槽位弧整体转 90°，所以"转后各方向 DPS"就是把
`MapEval.dir_dps` 循环移一位，不用重算几何。

**踩的坑**：给 `oracle` 开了"转向免费"，结果它比 `field` 差一半（214s vs 424s）。
原因是 AI 忽略了一个在 sim 里仍然生效的代价——转向那 1 秒真的不开火。
让 AI 无视真实代价不是优势是缺陷。去掉之后排序才正常。

**最终谱系**（每档 8 局）：stand 51s / line 94s / field 269s / oracle 369s，带宽 7.2 倍，
贴身占比 38.4% → 5.2%。详见 `docs/balance/AI策略调参记录.md`。

**两个量化结论**：
- 转向值 **+57%**（233s → 366s，其余参数不变）——§26-A 的第一个数据答案，
  前提是非对称 build；
- 转向滞回是最敏感的参数，转得太勤是最大的负优化（0.05/1.0 → 0.25/2.0 就是 +18%）。

**方差很大**：field 单档跨 66.5–787.6 秒，主因是发牌随机。看中位数，正式扫参数每格 ≥30 局。

**下一步 P6（金币 / 商店 / 卡牌 / 合并）**。理由：推演局长 26.7 分钟的前提是满配
38,400 DPS，实测最强只有 6.2 分钟——差距不在波次数值，而在玩家长不到满配。
现在的成长还是 P0 的经验升级占位。**P6 之前不应该拿实测局长去反推波次系数。**

## 2026-08-26：P6 金币 / 商店 / 卡牌 / 合并

经济闭环通了：掉落 → 拾取 → 商店生成 → 抽奖 → 买卡 → 装到底座 → 合并 → 解锁下一列。
`data/shop.json` + `sim/systems/{ShopSystem,CardPool}.gd`，替换掉 P0 的经验升级占位
（`UpgradeSystem.gd` 已删）。武器树的列/尺寸/卡价也一并修正了——
之前把 Spread Gun 和机枪错标成第 1 列，其实它们和 Rifle 一样是第 2 列的分支。

底座槽位从"3×3 写死 8 个偏移"推广成"格盘 + 尺寸"，支持 4×4 和 2×2 炮塔。

**头号发现：AoE 碾压单体，3.5 倍。** 8 门 Spread Gun 名义 DPS 只有 448，活 282 秒；
8 门 rifle 名义 2560 DPS，只活 81 秒。名义 DPS 翻 4 倍只多活 17%。
走完整循环对照三条线：spread 189s / rapid 90s / rifle 70s，**2.7 倍差距**。
详见 `docs/balance/P6_经济闭环与流派失衡.md`。

**经济参数不是瓶颈**：初始金币 200→2000、间隔砍半、距离拉近，存活只从 70→92 秒。
流派失衡不修，调经济没意义。

过程中修掉：§8"够买最少 2 张卡"读成了"两种最便宜的卡之和"（门槛高 3 倍，商店几乎不出现）；
AI 不去商店、不捡金币两个漏掉的行为；合并/放卡总落在第一个空位导致炮塔堆在一条边上。

**下一步的选择**：
1. 先补 **P5 装甲**（§6.2 五级树 + 装甲卡），底座近战是另一条独立的火力来源，
   也是 §26-D 要验证的；
2. 或者先处理**流派失衡**——这是当前最大的数值问题，但改法是设计决定
   （给单体线补机制？调范围武器的数值？改敌人曲线让单体有用武之地？）。

## 2026-08-26：P5 装甲（§6.2 五级树）

`data/armor.json` + Mech 的 armor_tier/armor_level 与四个派生值
（减伤 / 反击 / 光环每秒 / 光环范围），商店会出装甲卡，AI 会装到挨打最多的那一面。
SelfCheck 46 项全过，装甲数值逐项对上文档。

**§26-D 的答案是肯定的**：无炮塔 + 四面满级滚筒活 507.5 秒，
比最好的武器 build（8 门 Spread Gun，282.3s）还长 1.8 倍。
但前两级（装甲、尖刺）几乎无用，有输出的是链锯起的三级。

**更重要的是：所有 build 卡在同一个瓶颈——经济产出比造价低 1-2 个数量级。**
最便宜的强 build 要 4,800 金币，而玩家死亡时才攒到 850。
详见 `docs/balance/P5_装甲与所有build的共同瓶颈.md`。

**下一步该动的是数值，不是功能**：金币产出 ×5-10、或卡价 ÷5-10、
或前 2-3 个周期的敌人曲线大幅放缓。这三条都是设计决定，等确认。
